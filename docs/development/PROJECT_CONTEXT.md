# <PROJECT-NAME> — Project Context

> **Purpose of this file:** One-stop reference for AI agents working on this
> codebase. It covers what the project is, how it's built, where everything
> lives, and the key patterns in use. Agents read `DIGEST.md` first and come
> here only for the `##` section they need.

---

## 1. What This Project Is

<!-- INIT: what the app does, core jobs it performs, org context, external
     systems, where config/credentials live. -->

## 2. Technology Stack

<!-- INIT: layer-by-layer table with versions and notes. -->
| Layer | Technology | Notes |
|---|---|---|
| | | |

## 3. Repository Layout

<!-- INIT: annotated directory tree — deeper than the DIGEST version. Include
     entrypoints, registration points, and where generated/vendored code lives
     so agents don't edit it. -->
```
<repo>/
├── ...
```

## 4. Runtime / Startup

<!-- INIT: how services start (docker compose, make, scripts), ports, health
     endpoints, seed data, default credentials for local dev. -->

## 5. Configuration & Secrets

<!-- INIT: env var catalog or pointer, per-user vs global credentials, secret
     handling rules. -->

## 6. External Integrations

<!-- INIT: one short section per external system: base URL, auth method, which
     service module wraps it, and its mock seam for tests. -->

## 7. Data Model

<!-- INIT: key tables/collections and the services that own them. -->

## 8. Feature Map

<!-- INIT: table mapping feature area → docs/Features/<FILE>.md → main code
     paths. Keep updated as features ship. -->
| Feature | Doc | Code |
|---|---|---|
| | | |
