# Quality Gates — Validator Checklist

> **Moved.** This checklist now lives inline in `.opencode/agent/validator.md`
> (see "Review gates" section) so only the `validator` subagent
> pays the token cost of reading it — it is no longer a global instruction
> loaded into every agent's session. The validator checks each completed
> feature against the spec **and** the technical plan, embedded design, and QA
> test plan. This stub is kept so existing links don't break; treat
> `validator.md` as the source of truth and update it directly when the
> checklist changes.
